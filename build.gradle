buildscript {
    ext.curseforge_apiKey = System.getenv('curseforge_apiKey')
}

allprojects {
    repositories {
        jcenter()
        mavenCentral()
    }
}

subprojects {
    group 'eu.jsan.forge'
    version = "${project.mod_version}"
}
